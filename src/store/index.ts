import Vue from 'vue';
import Vuex from 'vuex';
import StoreType from './types';

Vue.use(Vuex);

export default new Vuex.Store<StoreType>({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
    },
});
